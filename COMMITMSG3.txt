security: purge committed secrets & make admin provisioning env-backed

- Remove a real PRIVATE KEY (mohameddosho20@gmail.com-*.pem) that was tracked
  despite the *.pem gitignore. The key is in git history, so it is considered
  compromised and MUST be revoked/reissued.
- create_superuser/fix_admin no longer hardcode the admin password; they read
  ADMIN_PASSWORD / DJANGO_SUPERUSER_PASSWORD from env and REFUSE to run without
  a password (prevents weak/default admin provisioning).
- Remove the committed admin password + credentials from README and AGENTS.md;
  document the env-backed provisioning instead.
- Untrack .playwright-cli/ DOM-snapshot test artifacts (contained the password);
  add .playwright-cli/ to .gitignore.

ACTION REQUIRED: rotate the Django admin password and revoke/reissue the leaked
private key in production.
